import React from 'react';

import * as S from './styled';

export const PredictLogBox: React.FC = () => {
    return (
        <S.PredictCont>
            <S.PredictBox>아우</S.PredictBox>
        </S.PredictCont>
    );
};
